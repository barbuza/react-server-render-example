@require "nib/reset"

global-reset()

body {
  > header {
    background: #eee;
    padding: 4px 8px;
  }
}

#workspace {
  padding: 20px;
}

#error404 {
  font-size: 300%;
}

#network-error {
  popup(400px, 100px);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 150%;
}

@require "./grid"
@require "./popup"
